<%= form_with(model: gank_session, local: true) do |form| %>
  <% if gank_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gank_session.errors.count, "error") %> prohibited this gank_session from being saved:</h2>

      <ul>
      <% gank_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
      <%= form.select :character_id,
      Character.select_options,
      {include_hidden: false, include_blank: false},
      {
        multiple: true,
        class: 'form-control',
        name: 'gank_session[gank_session_characters_attributes][][character_id]'
      } %>
  </div>

  <div class="actions">
    <%= form.submit 'Update', class: 'btn btn-primary form-control' %>
  </div>
<% end %>
